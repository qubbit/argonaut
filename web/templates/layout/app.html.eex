<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Reserve a testing environment">
    <%= if Guardian.Plug.current_token(@conn) do %>
    <meta name='guardian_token' content="<%= Guardian.Plug.current_token(@conn) %>">
    <meta name='time_zone' content="<%= Argonaut.User.current_user(@conn).time_zone %>">
    <% end %>
    <meta name="author" content="gopal adhikari">
    <title>🛳  Argonaut</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>
  <body>
    <div class="info-section"></div>
    <div class="container main-container">
      <div class="logo-container text-center">
        <a href="<%= index_path(@conn, :index) %>">
        <img class="logo-image" src="<%= static_path(@conn, "/images/argonaut.svg") %>">
        <span class="logo-text">Argonaut</span>
        </a>
      </div>
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>
    </div> <!-- /container -->
  </body>
</html>
